// Package rest builds the HTTP client the toolkit's API clients share.
// [NewClient] returns a client bound to a base URL, with the timeout and the
// retry policy every provider uses.
package rest
